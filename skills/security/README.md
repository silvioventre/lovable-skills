# security

Security of the app's own code: secrets, validation, access policies, authentication.

| Skill | What it does |
|---|---|
| [secure](secure/) | Routes the concern to the layer that owns it — frontend, edge function, row-level security, auth — and applies the rule that belongs there. Includes a ten-item gate to run before publishing. |

Add skills here as `skills/security/<skill-name>/SKILL.md` — see [`skills/_template/`](../_template/).
